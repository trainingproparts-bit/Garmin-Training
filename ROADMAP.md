# Roadmap

> Nota de numeração: as Sprints 1-4 já foram concluídas (ver `CHANGELOG.md`) — consolidação/implementação, migração de conteúdo, ciclo fechado de progresso, e o Motor de Avaliações Trimestrais. Este roadmap é revisado a cada sprint para refletir o que já foi resolvido (às vezes por código, às vezes por decisão de design) e o que surgiu de novo.

## Feito — Limpeza de Legado e Consolidação de Edge Functions (2026-07-13)

- [x] **Remoção definitiva de arquivos legados** — `login.html`, `cursos.html`, `js/auth.js` removidos após validação total da migração para o app Vite. Build do Vite (`npm run build`) confirmado funcionando sem erros. `index_redesign_v5.html` já não existia (provavelmente removido anteriormente).
- [x] **Edge Function `upload-attachment` implementada** — upload seguro de arquivos com validação de mime-type, tamanho máximo e quota antes de gerar URL assinada. Segue o mesmo padrão de segurança de `admin-create-user`: validação JWT, CORS headers, e proteção contra path traversal. Estrutura de pastas `supabase/functions/` validada para deploy via Supabase CLI.

## Feito — Configuração de Suíte de Testes com Vitest (2026-07-13)

- [x] **Vitest configurado e integrado** — ambiente jsdom configurado, scripts `"test"` e `"test:watch"` adicionados ao `package.json`. Mock do Supabase criado em `src/__tests__/mocks/supabase.js` para testes isolados sem depender de chamadas reais ao banco.
- [x] **30 testes automatizados implementados** — cobrindo regras de negócio críticas:
  - Regras de bloqueio da Trilha GPS (6 testes): validação de bloqueio sequencial em zonas não-free_order, liberação em zonas free_order, cálculo de progresso, identificação de próximo checkpoint, e marcação de opcionais.
  - Hardening de Quizzes e Games (10 testes): validação de chamadas RPC (`fn_submit_quiz_answer`, `fn_finalize_quiz_attempt`, `fn_submit_game_round`, `fn_finalize_game_session`) com tratamento de erros e retorno de dados.
  - Motor de Streak (12 testes): lógica de pausa de fim de semana (segunda/sábado/domingo usando sexta como piso, terça a sexta exigindo dia anterior).
- [x] **Todos os testes passando** — `npm test -- --run` executa 30 testes em 4.7s sem falhas.

## Feito — Configuração de Deploy e Leaderboard Trimestral (2026-07-13)

- [x] **Configuração para Vercel** — `vercel.json` criado com regras de SPA routing (todas as rotas redirecionam para `index.html` para evitar 404 ao recarregar páginas internas) e headers de cache otimizados (assets imutáveis com cache longo, HTML com cache curto). Estrutura pronta para deploy automatizado ao conectar o repositório.
- [x] **Variáveis de ambiente** — `.env.example` criado com template para `VITE_SUPABASE_URL` e `VITE_SUPABASE_ANON_KEY`. Configuração do Supabase (`src/config/supabase.js`) já usa `import.meta.env` para ler variáveis de ambiente, sem hardcoded values.
- [x] **Leaderboard Trimestral implementado** — `src/services/leaderboardService.js` criado com lógica de temporadas (Q1-2026, Q2-2026, etc.), funções para buscar ranking global/por loja/por cargo, histórico de temporadas com pódios, e lógica de fechamento de temporada (snapshot de ranking). Painel de histórico de temporadas adicionado ao `ranking.js` com visualização dos top 3 de cada temporada passada.

## Feito — Redesign visual premium (estilo streaming/LinkedIn Learning), 2026-07-10

Pedido explícito do usuário: sidebar retrátil, paleta premium, hero com anel de progresso circular, trilha/quizzes/conteúdo especial em carrosséis de cards 16:9 em vez de listas verticais. Implementado numa passada só (decidido com o usuário não pausar entre fases).

- [x] **Paleta e tokens** — `--g` atualizado pro vermelho exato pedido (`#E4002B`, era `#E31020`); novos `--bg-page`, `--sidebar-bg`, `--sidebar-w`/`--sidebar-w-collapsed`.
- [x] **Sidebar retrátil** — `src/components/icons.js` (15 ícones outline finos, sem depender de biblioteca nova); botão de colapsar com persistência em `localStorage` (`gth-sidebar-collapsed`), lido de forma síncrona antes do primeiro render pra não piscar; ícones coloridos trocados por outline em toda a navegação (Mural de Atividades mantém emoji — é conteúdo dele, não nav).
- [x] **Seção "Acesso Rápido" na sidebar** — Última Atividade/Novidades/FAQ, substituindo o grid que ficava solto no meio do Dashboard Principal (removido de `DashboardHome.js`). "Favoritos", citado como exemplo pelo usuário, virou "Novidades" — não existe sistema de favoritos no schema, seria feature nova fora de escopo de um redesign visual.
- [x] **Topbar novo** (`.app-topbar`, fixo acima da pilha de painéis) — sino de notificação real (finalmente dá uso ao motor que já existia desde uma sprint anterior e nunca tinha UI: `notifications` + `notificationService.js`) com badge de não-lidas e dropdown; avatar pequeno ao lado.
- [x] **Hero Card com anel de progresso circular** — `src/components/CircularProgress.js` (SVG puro, sem lib nova), substitui a barra linear fina como indicador principal de progresso da trilha.
- [x] **Trilha, Quizzes Extras e Linhas Especiais em carrossel de cards 16:9** — `src/styles/cards.css` (`.media-card`, compartilhado pelos 3 contextos). `GpsTrail.js` reescrito pra fileira horizontal por zona, preservando exatamente a lógica de bloqueio sequencial (`done`/`current`/`locked`, `zone.free_order`) e o contrato de clique — só o container visual mudou. Cards de módulo ganharam mini barra de progresso (fração de lições concluídas, nova query em lote em `trilhaService.fetchModuleProgressMap`).
- [x] **Linhas Especiais e Novidades** — `sql/seeds/060`/`061` (escritos desde a Sprint 2, nunca aplicados) finalmente ativados: 8 artigos reais (inReach, GPS de Mão, Náutico, Edge, Apps/Integrações, Novidades 2026, Blaze Equine, MARQ Gen 2) numa fileira única no Dashboard. Clique deep-linka pra Biblioteca Técnica já expandindo o artigo certo (`window.selectedLibraryCategory`/`selectedDeepDiveSlug`, novo mecanismo genérico de deep-link também usado pelo atalho de FAQ da sidebar).
- [x] **Capas dos cards (sem fotos reais ainda)** — nenhuma imagem existe hoje no banco/Storage pra módulos/quizzes/linhas especiais; cada card mostra gradiente + ícone até existir foto de verdade. `sql/027_cover_url_columns.sql` (coluna em `modules`/`quizzes`; `content_library.payload.cover_url` não precisou de coluna nova) + controle "Editar capa" admin-only (`window.prompt`) em Quizzes Extras e Linhas Especiais — testado ao vivo, capa salva e aparece no card.
  - **Bug real encontrado e corrigido durante o teste:** o topbar aparecia até na tela de escolha de marca (antes de logar numa marca) porque `updateSidebarProfile()` revelava o topbar incondicionalmente sempre que havia perfil logado, numa corrida com o hide-por-painel do router. Corrigido fazendo `router.js` ser a única fonte de verdade pro `hidden` do topbar (mesmo padrão já usado pro sidebar), removendo a lógica duplicada de `appShell.js`.
  - **Testado ao vivo com os 3 papéis e em mobile:** colaborador (Daniel) — sidebar colapsa e persiste após reload, sino mostra contagem real, trilha em carrossel preservando o bloqueio sequencial (card bloqueado sem clique, card atual clicável), fileira de Linhas Especiais abre o artigo certo na Biblioteca; admin (Samara) — "Editar capa" salva e persiste (revertido depois); líder (Ailma) — nav com os itens certos (sem Painel Admin). Sem erros de console em nenhum papel. Mobile (375px): sidebar some (breakpoint já existia), hero empilha (anel acima do texto), cards estreitam, sem overflow horizontal de página.

## Feito — última rodada (Usuários & Papéis), 2026-07-09/10

- [x] **`sql/008_profiles_privilege_escalation_fix.sql`** — aplicado e confirmado ao vivo em produção.
- [x] **Popular a tabela `stores`** — 4 lojas (Morumbi/Moema × Garmin/Shokz), 14 profiles, 3 vínculos em `store_leaders`, tudo verificado ao vivo (`sql/010`).
- [x] **Testar Dashboard do Líder e Painel Admin com login real** — feito com Samara (admin) e Ailma (líder); achou e corrigiu mais 2 bugs reais pré-existentes no caminho (embed ambíguo do PostgREST, recursão de RLS em `fn_is_admin`/`fn_is_leader` — `sql/013`).
- [x] **Cadastro de novo usuário pelo admin (Edge Function)** — `supabase/functions/admin-create-user`, primeira Edge Function do projeto. Testado ao vivo: cadastro + login real do usuário criado funcionaram de ponta a ponta.

## Agora — sem bloqueio de dependência

- [x] **Alternativa em disputa da RN 1.1 — decidida (2026-07-10):** Líder **não** cadastra usuário, só Admin. Encerrado, não vira código.
- [x] **Testar o fluxo autenticado completo de colaborador (2026-07-10):** login → lição → conclusão → checkpoint de quiz desbloqueado → aprovação → certificado emitido → avaliação trimestral — percorrido de ponta a ponta pela primeira vez, com Daniel Lucena (Morumbi), usando o app real (não SQL sintético): 3 lições de "O Universo Garmin" concluídas com pontos reais, quiz aprovado (100%, RPC real), checkpoint seguinte desbloqueado — repetido pelos 4 módulos+quizzes da Zona Explorador (8 checkpoints), certificação "Explorador" emitida automaticamente, Avaliação Trimestral Explorer aprovada (100%). Achou e corrigiu **4 bugs reais pré-existentes**, nenhum introduzido nesta sprint, todos nunca detectados por falta desse teste de ponta a ponta:
  1. `fn_issue_certification()` contava checkpoints da trilha inteira, não da zona da certificação (`sql/017`).
  2. `trg_issue_certification` só disparava em `UPDATE`, nunca em `INSERT` — certificação nunca era emitida na prática (`sql/018`... `sql/019`).
  3. `fn_update_user_progress_from_lesson()` destravava o quiz já na 1ª lição do módulo, não esperava as outras (`sql/018`).
  4. Quiz lançado como checkpoint da trilha sempre devolvia pro painel "Quizzes Extras" ao terminar, nunca pra "Minha Trilha" — a tela de trilha parecia não atualizar o checkpoint recém-desbloqueado (era isso, não cache) — corrigido em `src/pages/quizRunner.js`/`trilha.js`/`quizzes.js`.
  Progresso de teste no Daniel revertido ao final (não é dado de produção real).
- [x] **Regenerar `dist/`** *(2026-07-10)* — bundle novo: JS 288.14 kB (gzip 71.15 kB), CSS 34.61 kB (gzip 7.03 kB). Cresceu em relação ao build de 2026-07-08 (251.78/64.04 kB JS, 32.40/6.66 kB CSS) — esperado, entrou bastante feature nova desde então (Painel Admin completo, Relatório de Gaps, Edge Function de cadastro).
- [x] **Hardening de Games** *(2026-07-10)* — ver "Quizzes & Games" no `PROJECT_CHECKLIST.md`: achado que o recurso estava **quebrado** (não só desprotegido) e corrigido com o mesmo padrão RPC `SECURITY DEFINER` dos quizzes (`sql/021_game_submission_hardening.sql`).
- [x] **Mural de Atividades** *(2026-07-10)* — ver "Usuários & Papéis" no `PROJECT_CHECKLIST.md`: `sql/022_activity_feed.sql` (tabela + RLS + triggers de badge/certificação + `fn_leader_post_activity`), painel "Atividades Recentes" com Realtime no Dashboard Principal, seção "Destaques do Balcão" (templates fixos) no Dashboard do Líder. Achou e corrigiu um bug real de UX (campo `required` escondido bloqueava o submit nativo silenciosamente para líder de loja única). Escopo mais estreito que a modelagem original (só badge/certificação como gatilho automático — streak/nota 100% ficam para quando houver hook equivalente).
- [x] **Engine de concessão automática de badges** *(2026-07-10)* — `sql/023_badge_granting_engine.sql`: fecha o gatilho do Mural que ficou "morto" no item anterior (nada inseria em `user_badges` ainda). Explorer/Runner na emissão da certificação de zona; Triathlete quando ambas emitidas (trilha real completa); Gabarito Garmin na 1ª tentativa de quiz com 100%. Testado ao vivo com dado sintético nos 4 caminhos (Daniel Lucena), revertido ao final — inclusive o efeito colateral de XP que o `fn_award_points_on_pass` (schema base) disparou por causa do quiz sintético. Ritmo Constante fica de fora — depende de sistema de streak que não existe.

## Depois — Editor de conteúdo rico

Depende de Storage Buckets (já criados) pra mídia funcionar de verdade dentro do editor.

- [x] **Schema de blocos para `lessons.body`/`content_library.payload`** *(2026-07-10)* — subconjunto essencial de 8 tipos decidido com o usuário (dos 20 documentados na Fase 4 §6.6): banner, texto rico, accordion, card, timeline, vídeo, galeria, quiz embutido. `src/components/ContentBlocks.js` (render + editor de formulário estruturado, sem drag-and-drop) + `sql/024_lesson_content_blocks_migration.sql` (migra as 38 lições reais de `{"html": "..."}` único pra `{"blocks": [...]}`, sem perda de conteúdo). Editor testado ao vivo (admin real): adicionar/remover bloco, salvar, recarregar, editar de novo — tudo persistindo corretamente. Ficam para uma rodada futura: os 12 tipos restantes (flip card, comparativo, tabela, avaliação, flashcards, cenários, slider, calculadora, checklist, downloads, grid, destaque) e o editor drag-and-drop completo da Gestora.
  - **2 bugs reais pré-existentes encontrados e corrigidos durante o teste:** (1) o botão "Editar conteúdo" aparecia pra qualquer usuário logado, mas `lessons` só tem RLS de `UPDATE` pra admin (`lessons_admin_all`) — líder/colaborador clicava, editava e o salvamento falhava silenciosamente (406, sem mensagem clara). Corrigido escondendo o botão para quem não é admin. (2) `moduleService.fetchModuleWithLessons` nunca selecionava `lessons.module_id`, então o recarregamento do módulo após salvar (`fetchModuleWithLessons(lesson.module_id)`) sempre quebrava com `id=eq.undefined` — nunca tinha sido exercitado porque nenhum save chegava a completar antes do bug 1 ser corrigido. Corrigido passando `module.id` (já disponível em `renderModule`) em vez de depender do campo ausente.
  - **Achado colateral, fora de escopo, não corrigido:** o módulo "Garmin Connect" tem cada uma das 4 lições triplicada em `lessons` (12 linhas, 3 IDs por título) — bug de dados pré-existente, não causado por nenhuma migração recente. Investigação e limpeza ficaram para uma tarefa separada.
- [x] **+4 tipos de bloco: `roteiro`, `objecao`, `tabela`, `card_grid`** *(2026-07-10)* — os 8 artigos de "Linhas Especiais" (`content_library`, categoria `deep_dive`) tinham sido migrados como um único bloco `texto_rico` de prosa corrida, perdendo a estrutura visual rica do protótipo `index_redesign_v5.html` (scripts de venda com fala literal, blocos de objeção, tabelas comparativas, cards com tags). `sql/028_deep_dive_rich_blocks.sql` reconstrói os 8 artigos linha a linha do protótipo usando os 4 tipos novos — `roteiro` (passos numerados + fala em destaque com botão "Copiar Argumento" + dica), `objecao` (pergunta do cliente + resposta), `tabela` (comparativo em colunas) e `card_grid` (grade de 2-3 colunas com tags coloridas, reaproveitando `.tag`/`.tag-blue/green/orange/gold` já existentes). Testado ao vivo. Acompanha ajuste de densidade CSS (`shell.css`/`modulo-content.css`/`contentBlocks.css`) que elimina uma camada de padding duplicada no painel de lição. Ficam para rodada futura: os 8 tipos restantes e as 40 lições dos 6 módulos reais (fora de escopo — dependem de widgets sob medida por módulo: flip card, simulador, diagnóstico em árvore).
- [x] **Página dedicada por guia técnico + abas internas (itabs)** *(2026-07-10)* — os 8 guias viviam como itens de accordion dentro da própria Biblioteca Técnica (rolagem comprida, sem navegação interna). Novo painel `deep-dive-detail` (`src/pages/deepDiveDetail.js`, mesmo padrão de `modulo-conteudo.js`: back button, título dinâmico) dá a cada guia sua própria página. Os 3 guias mais densos — inReach, Edge, Apps/Integrações — reaproveitam o seletor de abas (`.itabs`/`.itab`) do protótipo original (`sql/029_deep_dive_tab_groups.sql` reagrupa os blocos existentes em `{ intro, tabs: [{label, blocks}] }`, sem alterar texto); os outros 5 continuam em rolagem única, igual ao protótipo original (nunca tiveram abas). `LibraryContent.js` trocou o accordion de Guias Técnicos por uma lista simples de links (`navigateToPanel('deep-dive-detail')`); o Dashboard ("Linhas Especiais") deep-linka direto pro painel, sem passar pela Biblioteca. **Bug real encontrado e corrigido no processo:** `renderTextoRicoBlock` (`ContentBlocks.js`) sempre envolve o HTML em `<div class="content-article-body">`, cujo CSS (`modulo-content.css`) usava texto branco/cinza-claro pensado só pro card escuro da lição — dentro do painel claro do guia técnico isso ficava branco-sobre-branco (H3 invisível). Corrigido escopando essas regras a `.content-article .content-article-body` (só a lição escura), preservando o comportamento original ali.
- [x] **Reconstrução das 22 lições reais dos 6 módulos + tipo de bloco `flip_card`** *(2026-07-11)* — mesma correção estrutural de deep-dives, agora nos módulos de treinamento (Universo Garmin, Perfis de Cliente, Portfólio de Produtos, Concorrentes & Objeções, Garmin Connect, Garmin Coach). `sql/030_module_lessons_rich_blocks.sql` reestrutura as 22 lições (achatadas em `texto_rico` único) usando os tipos de bloco já existentes — inclui a árvore de decisão de 7 desfechos do Garmin Coach virando uma `tabela` "situação → plano indicado". `sql/031_dedupe_connect_coach_lessons.sql` corrigiu um bug de dados pré-existente que fazia cada lição de Garmin Connect **e** Garmin Coach aparecer 3× seguidas na tela (só Connect estava documentado; Coach tinha o mesmo bug). **Correção de rota em cima do feedback do usuário:** a primeira versão desta rodada tinha achatado os flip-cards do protótipo (clique pra virar e ver o verso — usados em Novidades 2026, no estudo de caso do Garmin Connect e nos treinadores do Garmin Coach) em `card_grid`/`roteiro` estáticos, perdendo a interação; `sql/032_flip_card_widgets.sql` introduz o tipo de bloco `flip_card` (mesma técnica CSS 3D do `.cfi`/`.trainer-card` original — `perspective` + `transform-style: preserve-3d` + `backface-visibility: hidden`) e reconstrói os 3 pontos com a interação de fato. O usuário também rejeitou o fundo escuro do card de lição (`.content-article`, herdado da fase de redesign visual anterior) por prejudicar a leitura de texto longo — revertido para fundo claro/texto escuro (só elementos coloridos/acentos permanecem coloridos), preferência a manter daqui pra frente.
- [x] **Sidebar compacta + menu do avatar** *(2026-07-11)* — a sidebar tinha 11 links + Acesso Rápido + rodapé (precisava de scroll pra ver tudo). Dashboard do Líder, Relatórios e Painel Admin saíram do `sb-nav` e foram pro menu do avatar no topbar (mesmo padrão do notif-dropdown — abre ao clicar, fecha ao clicar fora), junto com Modo escuro e Sair (que também saíram do rodapé da sidebar). `NAV_ITEMS` ganhou um campo `location: 'avatar'`; `SIDEBAR_NAV_ITEMS`/`AVATAR_NAV_ITEMS` derivam da mesma lista, então clique/estado ativo/visibilidade por papel (`revealRoleScopedNav`) continuam funcionando sem mudanças em `router.js` — só mudou onde cada `.sb-link[data-panel]` é renderizado. A logo (`logo-branco.png` + wordmark "Training Hub") voltou ao topo da sidebar, ausente desde a reescrita da Sprint 1. Paleta da sidebar: preto verdadeiro (`--sidebar-bg: #0a0a0a`, era um azul-acinzentado `#111214`/`#1c1c26`) + texto branco + acentos vermelhos — chrome de navegação continua escuro por decisão do usuário, só o *conteúdo* (lições/guias) é que é claro (ver item anterior). **3 bugs reais pré-existentes encontrados e corrigidos:** `.sb-link[hidden]`, `.avatar-dropdown-action[hidden]` e `.notif-badge[hidden]` não escondiam de fato o elemento — uma regra CSS de origem *author* com `display` (ex.: `.sb-link{display:flex}`) sempre vence a regra `[hidden]{display:none}` da *user-agent stylesheet*, não importa a especificidade do seletor (mesmo problema que `.app-sidebar[hidden]`/`.app-topbar[hidden]` já tratavam explicitamente). Sem isso, os links restritos por papel, o badge de notificação e o botão Sair ficavam com `hidden=true` mas continuavam visíveis pra qualquer usuário — corrigido com `[hidden]{display:none}` explícito nos 3 seletores.
- [x] **Painel da Gestora — 1ª rodada** *(2026-07-12)* — pedido direto do usuário, escopo específico (não o CRUD completo de módulos/lições/quizzes com drag-and-drop, ver 2ª rodada abaixo): novo painel admin-only (`src/pages/gestoraPanel.js`, nav item no menu do avatar) com 2 blocos.
  - **Postar no Blog sem sair do painel** — reaproveita `blogService.js` (mesma tabela/RLS de `blog.js`, não é um 2º caminho de postagem): formulário compacto + lista dos 5 posts mais recentes. Editar/excluir um post existente continua no painel Blog (não duplicado aqui).
  - **Relatório de Quizzes por Loja** — `src/services/gestoraService.js`, `fetchAllQuizAttemptsReport()`: todas as tentativas de quiz já finalizadas, org inteira, agrupadas por loja no cliente. Sem view/tabela nova — `quiz_attempts_admin_all` (RLS já existente, `ALL` sem escopo de loja) já libera isso pra admin; é o mesmo caminho de `fetchTeamQuizAttempts` (`teamService.js`), só sem o `.limit(20)`.
  - **Testado ao vivo com login real (Samara, admin):** publiquei um post de teste pelo formulário do painel, confirmado gravado em `blog_posts` com `author_id` correto e visível na lista "Últimos posts" — apagado depois. Inseri 2 tentativas de quiz sintéticas (Daniel/Morumbi aprovado 85%, Dayane/Moema reprovado 40%) e o relatório separou corretamente as 2 lojas, com nota/resultado/data certos — revertido ao final junto com os efeitos colaterais dos triggers de pontos/streak/mural (`sql/033`/`035`) que a inserção sintética disparou.

- [x] **CRUD de módulos/lições/quizzes com drag-and-drop (Gestora — 2ª rodada)** *(2026-07-12)* — pedido em seguida pelo usuário ("pode fazer o crud"). `src/services/contentAdminService.js` (toda a camada de dados) + `src/pages/gestoraContentEditor.js` (UI, montada como 3º bloco dentro do Painel da Gestora, 3 abas — mesmo padrão `.itabs`/`.itab` dos guias técnicos densos). Nenhuma migração/RLS nova: `modules_admin_all`/`lessons_admin_all`/`quizzes_admin_all`/`questions_admin_all`/`alternatives_admin_all` (todas `ALL`, `fn_is_admin()`) já existiam desde o schema base — só faltava a UI.
  - **Módulos**: criar (zona/título/resumo/minutos), editar **todos os 4 campos** (zona/título/resumo/minutos — não só o título; achado pelo usuário testando na hora: "só consigo adicionar o nome" — a 1ª versão editava só título inline, corrigido pra expandir a linha inteira num formulário com os 4 campos, incluindo trocar de zona, que reposiciona o módulo no fim da lista da zona nova), publicar/despublicar, excluir, reordenar por arrastar-e-soltar dentro da zona (drag nativo HTML5, sem lib nova).
  - **Lições**: criar/editar título/publicar/excluir/reordenar dentro do módulo selecionado; botão "Editar conteúdo →" leva direto pro editor de blocos já existente (`ContentBlocks.js`, painel Conteúdo do Módulo) — não duplicado aqui, essa tela só administra a lição em si.
  - **Quizzes**: criar/editar/excluir + perguntas e alternativas aninhadas (marcar a resposta correta via rádio, `fn_enforce_single_correct_alternative` do schema base desmarca as demais automaticamente).
  - **Fora de escopo por design do schema** (não é lacuna desta rodada): vincular um módulo/quiz novo à trilha real (criar o `checkpoint` correspondente) continua sendo uma ação separada — módulo/quiz é conteúdo, checkpoint é posicionamento na trilha, tabelas distintas de propósito. Os 20 tipos de bloco documentados na Fase 4 também seguem fora de escopo (hoje só os 13 já existentes em `ContentBlocks.js`).
  - **2 bugs reais encontrados e corrigidos em revisão de código, antes mesmo do teste ao vivo:** (1) drag-and-drop persistia a nova ordem no banco mas não atualizava o `order_index` em memória, então a re-renderização imediata desfazia o arrasto visualmente. (2) edição inline de título (módulo/lição) empilhava um listener novo a cada clique em "Editar" sem remover o anterior — no 2º clique, "entrar em edição" e "salvar" disparavam juntos, arriscando salvar o valor errado. Reescrito com um único listener por botão, alternando estado via `dataset.mode`.
  - **1 bug cosmético encontrado e corrigido durante o teste ao vivo:** `createQuiz()`/`createModule()` devolvem só as colunas cruas (sem o embed `brands(name)`/`zones(name)`), então o item recém-criado mostrava "—" no lugar do nome da marca/zona até o próximo fetch. Corrigido completando o objeto em memória com o registro já carregado do dropdown antes de inserir na lista.
  - **Testado ao vivo, ponta a ponta, com login real (Samara, admin), sobre as 4 disciplinas reais da Zona Explorador (Garmin) — nada de produção alterado ao final:** criei um módulo de teste, publiquei, editei o título inline (confirmando o bug 2 corrigido — salvou certo no primeiro "Salvar"), arrastei pra trocar de posição com um módulo real e revertido, confirmando a ordem original restaurada exatamente; criei uma lição de teste dentro dele, confirmei que ela **não** aparece na tela real de conteúdo enquanto despublicada (RLS/filtro corretos), publiquei e confirmei que passa a aparecer; criei um quiz de teste com 1 pergunta e 2 alternativas, marquei uma correta, troquei pra outra e confirmei que a 1ª desmarcou sozinha (exclusividade do trigger); excluí quiz (cascata de pergunta+alternativas), lição e módulo — zero erro no console durante toda a sessão.
- [ ] **Ficha detalhada de produto** (imagem, galeria, specs completas) — mesma dependência de mídia.
- [x] **Álbum da Equipe** *(2026-07-12)* — pedido direto do usuário ("precisa transferir o album da equipe pro novo index"), portado de `index_redesign_v5.html`. `sql/037_team_album.sql` + `src/services/teamAlbumService.js` + `src/pages/album.js` + `src/styles/album.css`, nav item novo no sidebar (brand-scoped, como Ranking).
  - **Decisão de escopo tomada com o usuário:** dos atributos do card original, vários nunca tiveram fonte de dado real — o próprio protótipo admitia isso em comentário (Ritmo/Reputação vinham de planilha externa, preenchidas manualmente). Opções apresentadas: versão enxuta só com dado real, vs. manter tudo e deixar editável. Usuário escolheu manter tudo, exceto "Selo" (faixa de raridade ⭐⭐⭐⭐⭐, puramente editorial, sem nenhum significado de progresso) — removido.
  - **Mapeamento pra dado real:** Produto = % de lições publicadas concluídas; Precisão = média de nota de quiz; Jogo = média de accuracy de game; Classe = certificação mais alta emitida (Explorador/Corredor/Maratonista/Triatleta — os 4 reais; "Ultramaratonista" do protótipo não existe como certificação de verdade, não portado); Ritmo = aproximação via percentil de `performance_score` (documentado como aproximação — não é posição de vendas real, essa fonte não existe no schema). Emoji/frase (colunas já existiam, nunca tinham UI) e 3 colunas novas (especialidade/relógio favorito/esporte) são autoeditáveis — mesmo grupo de "identidade pessoal" que `fn_guard_profile_self_update` (sql/008) já liberava. Reputação/Ponta do Mês (2 colunas novas) são dado curado, só admin edita — o guard trigger foi estendido pra bloquear autoedição desses 2 campos também.
  - **`v_team_album`** — view pública estreita (mesmo padrão de `v_ranking_public`, sql/026): qualquer colaborador vê a equipe inteira da própria marca, admin vê tudo.
  - **1 bug de segurança real encontrado e corrigido em revisão de código, antes de qualquer teste:** o fallback de avatar usava um atributo `onerror` inline interpolando o emoji do usuário (campo de texto livre, sem validação) direto dentro de uma string de handler JS — um vetor de XSS mais sério que interpolação simples em innerHTML (que já é uma lacuna pré-existente em todo o app, não introduzida agora). Corrigido anexando o listener de erro depois de inserir no DOM (`wireAvatarFallbacks`), sem nenhuma string de usuário virando código.
  - **Bloqueio real de infraestrutura durante a sessão:** o servidor MCP do Supabase caiu no meio do desenvolvimento (não relacionado ao código) — sem acesso pra aplicar a migração diretamente, o usuário rodou `sql/037` manualmente no SQL Editor do Supabase; retomei o teste ao vivo assim que confirmado.
  - **Testado ao vivo, ponta a ponta, com login real, dado real da Garmin (11 colaboradores) — revertido ao final:** grade renderizou as 11 figurinhas corretamente (loja, classe, atributos); como Samara (admin) marquei o card do Daniel como "Ponta do Mês" com reputação 77, confirmei que a faixa 🔥 apareceu na grade e o valor persistiu reabrindo o modal — revertido; como Daniel (colaborador) editei minha própria figurinha (emoji/especialidade/relógio/esporte/frase), confirmei que salvou e apareceu no modal de detalhe — revertido ao estado original; **confirmei o guard de segurança bypassando a UI**: chamei `updateCuratedAlbumFields` como Daniel direto no console tentando setar a própria reputação — bloqueado corretamente pelo trigger, mesma mensagem de erro do guard original (sql/008). Admin (Samara) corretamente não aparece no álbum de nenhuma marca (não tem loja/marca atribuída — mesmo comportamento real dos 3 admins do sistema).

## Depois — Gamificação social

Tecnicamente independente do resto — pode ser adiantada se a prioridade de negócio mudar.

- [x] **XP e livro-razão de pontos** *(2026-07-12)* — `sql/035_game_and_streak_xp.sql`: as 2 fontes de XP que faltavam de RN §6.1 ("participação em game", "streak mantido") agora lançam em `points_ledger`. Game: 50 pts (valor recomendado na RN) na 1ª sessão finalizada de cada game por usuário, sem repetir em replay. Streak: 20 pts a cada marco de 5 dias, no mesmo evento que já posta no Mural/concede o badge Ritmo Constante (`fn_touch_streak`, `sql/033`). Quiz/lição já lançavam desde o schema base/`sql/004`; badge/certificação não geram XP direto por design (RN §6.10 — o XP já veio do evento original, o Mural é só reconhecimento social). Testado ao vivo (Daniel Lucena, revertido): `performance_score` bateu 70 (50 do game + 20 do streak) antes da limpeza.
  - **Falha de autorização real encontrada e corrigida em `sql/036`:** ao testar `035`, percebi que a revogação de `EXECUTE` feita em `sql/034` **não tinha funcionado de verdade** — `anon`/`authenticated` continuavam com acesso porque o grant efetivo vinha de `PUBLIC` (concedido automaticamente a toda função nova pelo Postgres), e revogar de um papel específico não remove o que ele herda de `PUBLIC`. Corrigido revogando de `PUBLIC` nas 9 funções (as 8 de `034` + a nova `fn_award_points_on_game_finish`). Confirmado ao vivo com `has_function_privilege`: agora retorna `false` pra `anon`/`authenticated` nas 9, e os triggers continuam funcionando normalmente.
- [x] **Badges** — `sql/022` (5 registros nomeados + trigger de Mural) e `sql/023` (engine de concessão: Explorer/Runner/Triathlete por certificação de zona, Gabarito Garmin por quiz 100% na 1ª tentativa) fecham o loop de ponta a ponta.
- [x] **Streak** *(2026-07-11)* — `sql/033_streak_engine.sql` fecha a lacuna: engine reativa (sem job diário — a tabela `streaks`/`fn_update_streak()` do schema base já existiam, mas nada nunca populava `study_sessions` nem chamava a função, código morto desde sempre). `fn_touch_streak` roda a cada lição/quiz/game/avaliação concluídos (4 triggers), com pausa de fim de semana (RN §6.5) e `v_streaks_effective` recalculando na leitura se o streak ainda está vivo. Marco de 5 dias posta no Mural e concede o badge Ritmo Constante (fechando a lacuna deixada em `sql/023`). Pill "🔥 N dias seguidos" no Dashboard Principal (`streakService.js` + `DashboardHome.js`). `sql/034_revoke_internal_only_rpcs.sql` corrige, no mesmo lote, uma falha de autorização real (achada pelo advisor do Supabase): `fn_touch_streak` e as 3 funções de concessão de badge (`023`, pré-existentes) podiam ser chamadas direto via RPC por qualquer usuário com um `p_user_id` arbitrário — `EXECUTE` revogado de `anon`/`authenticated` nas 8 funções.
- [x] **Ranking de Pontos + Destaque de Vendas por loja** *(2026-07-10)* — item de menu novo "🏆 Ranking" (`src/pages/ranking.js`), visível a todos após escolher marca. Ranking ao vivo por `profiles.performance_score` (não é a tabela `leaderboard`, que fica reservada pra quando existir temporada trimestral/medalhas — RN §6.4/6.6, fora de escopo aqui por decisão do usuário). "Destaque de Vendas": card por loja com texto curto, atualizado manualmente por Admin (qualquer loja) ou Líder (só a própria, via `fn_leader_store_ids()`) — pedido direto do usuário, não documentado na RN. `sql/025_ranking_store_highlights.sql` + `sql/026_ranking_public_view.sql`.
  - **Bug real encontrado e corrigido durante o teste ao vivo:** `profiles` só tem RLS de leitura pra si mesmo/líder da própria loja/admin — um colaborador comum via o ranking com 1 linha só (a dele). Corrigido com `v_ranking_public`, view estreita expondo só nome/pontuação/loja (mesmo padrão de outras views públicas do schema).
  - **Testado ao vivo com os 3 papéis:** admin (Samara) edita destaque de qualquer loja; líder (Ailma, Morumbi) só vê o botão Editar na própria loja — tentativa de editar Moema via chamada direta (bypassando a UI) foi bloqueada pela RLS, não só escondida na interface; colaborador (Daniel) vê as 11 pessoas no ranking corretamente após a correção da view, sem botão de editar destaque.

## Depois — Funcionalidades do objetivo final ainda não especificadas

Nunca foram detalhadas em nenhum documento de regras de negócio ou UX — precisam de uma rodada de definição antes de virar código.

- [x] **Blog** *(2026-07-10)* — renomeado de "Blog Casos Reais" a pedido do usuário: não é exclusivo de casos reais, "Caso Real" é uma das 4 categorias. `src/pages/blog.js` + `src/services/blogService.js` sobre a tabela `blog_posts` já existente no schema base (categoria fixa via CHECK: Caso Real/Novidade/Comunicado/Dica; leitura pra qualquer autenticado; autoria só Admin — RLS já existia, sem UI até agora). Item de menu novo "📰 Blog", não é por marca (tabela não tem `brand_id` — conteúdo da organização inteira, visível em ambas as marcas). Testado ao vivo: Admin (Samara) cria/edita post; colaborador (Daniel) vê o post publicado sem nenhum botão de admin.
  - **Achado, não corrigido (fora de escopo desta rodada):** `blog_posts_select_authenticated` deixa qualquer autenticado ler **todas** as linhas via API direta, publicadas ou não — a UI só busca rascunhos pra quem é admin, mas a RLS em si não impede um colaborador de consultar a tabela direto e ver rascunhos. Risco baixo (conteúdo de blog, não dado sensível), mas é uma lacuna real de RLS pré-existente no schema base, não introduzida agora.
- [ ] **Busca inteligente** — sem especificação de escopo.
- [ ] **IA especialista Garmin** — depende de decisão de arquitetura. Precisa de discovery próprio antes de estimar.

## Adiado a pedido do usuário

- **Performance** (Lighthouse, lazy-load) — baixa prioridade enquanto o app ainda muda de forma estrutural a cada sprint.
